# Provider Account Fixtures

This directory is a fake user home for provider account collection tests.

The credential and gateway files are structurally loadable by local agents, but
all tokens and API keys are synthetic test values and are expected to fail if
sent to upstream services.
